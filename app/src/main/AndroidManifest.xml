<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- v6.0.0: Permissão para UsageStatsManager (Ghost Mode polling) -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <!-- v6.0.0: Vibração para alerta durante Ghost Mode -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- v6.7.0: GPS em background para rastreamento de KM durante turno -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- v6.7.0: Acelerômetro e sensores para detecção de movimento -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <!-- v6.7.0: Iniciar automaticamente após reiniciar o celular -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- v6.7.0: Alarmes exatos para lembretes de manutenção -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:name=".NGBAutoRoadApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NGBAutoRoad"
        tools:targetApi="34">

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.NGBAutoRoad">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Card Gallery Activity -->
        <activity
            android:name=".ui.gallery.CardGalleryActivity"
            android:exported="false"
            android:theme="@style/Theme.NGBAutoRoad" />

        <!-- Card Editor Activity (Custom Card 3) -->
        <activity
            android:name=".ui.editor.CardEditorActivity"
            android:exported="false"
            android:theme="@style/Theme.NGBAutoRoad" />

        <!-- Admin Activity (oculta) -->
        <activity
            android:name=".ui.admin.AdminActivity"
            android:exported="false"
            android:theme="@style/Theme.NGBAutoRoad" />

        <!-- Finance Activity -->
        <activity
            android:name=".ui.finance.FinanceActivity"
            android:exported="false"
            android:theme="@style/Theme.NGBAutoRoad" />

        <!-- Zone Map Activity -->
        <activity
            android:name=".ui.map.ZoneMapActivity"
            android:exported="false"
            android:theme="@style/Theme.NGBAutoRoad" />

        <!-- Overlay Service -->
        <service
            android:name=".service.OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse|location" />

        <!-- OCR Capture Service -->
        <service
            android:name=".service.OcrCaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- Bubble Service (botão flutuante lateral) -->
        <service
            android:name=".service.BubbleService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Accessibility Service (v6.0.0 Triple Engine) -->
        <!-- v6.2.0: isAccessibilityTool=true protege contra bloqueio pelo AAPM no Android 16/17 -->
        <service
            android:name=".service.RideAccessibilityService"
            android:exported="true"
            android:isAccessibilityTool="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- v6.0.0: Notification Listener Service (Camada 3 — backup durante Ghost Mode) -->
        <service
            android:name=".service.RideNotificationListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity android:name=".ui.features.FeaturesActivity" android:exported="false" />

        <!-- v6.1.0: BroadcastReceiver para ações da notificação UNCERTAIN -->
        <receiver
            android:name=".service.UncertainReceiver"
            android:exported="false" />

        <!-- v6.8.0: Auto-start após reboot -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>



        <!-- v6.9.8: FileProvider para exportação de telemetria, logs e critérios -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- TestReceiver for simulation -->
        <receiver
            android:name=".service.TestReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.ngbautoroad.ACTION_SIMULATE_RIDE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
